export interface IPermit {
    id: number
    name: string
    location: string
    code: string
    time: string
    contact: string
    purpose: string
    fee: string
    amount_paid: number
    outstanding_fee: number
    status: number
    remark: string
    resolved: number
    permit: string
}